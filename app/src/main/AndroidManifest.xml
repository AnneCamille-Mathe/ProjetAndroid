<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="fr.eseo.acm.andoird.projetandroid">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Navigation.ProjetActivity"/>
        <activity android:name=".Navigation.Connexion"/>
        <activity android:name=".Navigation.JuryActivity"/>
        <activity android:name=".Navigation.ComNotesActivity"/>
        <activity android:name=".Navigation.ChoixMenus"/>
        <activity android:name=".API.API"
            tools:ignore="Instantiatable" />
        <activity android:name=".API.Enum"
            tools:ignore="Instantiatable" />

        <activity android:name=".MainActivity">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>

